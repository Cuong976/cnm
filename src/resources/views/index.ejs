<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="index.css" />
    <!-- CSS only -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.1/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-iYQeCzEYFbKjA/T2uDLTpkwGzCiq6soy8tYaI1GyVh/UjpbCx/TYkiZhlZB6+fzT" crossorigin="anonymous">
    <title>GK Cong Nghe Moi</title>
</head>

<body>
    <div class="container mt-3">
        <form action="/" method="post" enctype="multipart/form-data" class="form-inline">
            <input type="text" placeholder="enter id" name="product_id" class="form-control">
            <input type="text" placeholder="enter name" name="name" class="form-control">
            <input type="text" placeholder="enter price" name="price" class="form-control">
            <input type="text" placeholder="enter quantity" name="quantity" class="form-control">
            <input type="submit" value="Submit" class="btn btn-primary"> 

        </form>
    </div>
    <br />
    <div class="container">
        <table class="table table-success table-striped-columns">
            <thead>
                <th>Id</th>
                <th>Name</th>
                <th>Quantity</th>
                <th>Price</th>
                <th>Option</th>
            </thead>
            <tbody>
                <% products.forEach((product)=>{ %>

                    <tr>
                        <td>
                            <%= product.product_id %>
                        </td>
                        <td>
                            <%= product.name %>
                        </td>
                        <td>
                            <%= product.quantity %>
                        </td>
                        <td>
                            <%= product.price %>
                        </td>
                        <td>
                            <a href="">update</a> |<a href="/delete/<%= product.product_id %>">delete</a></td>
                    </tr>

                    <% });%>
            </tbody>
        </table>
    </div>
</body>

</html>